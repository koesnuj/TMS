# TMS (Test Management System)

Vibe Coding 스타일로 구축된, **Next.js 14** 기반의 자체 테스트 관리 시스템입니다.
TestRail 없이도 테스트 케이스를 체계적으로 관리하고, 실행 결과를 추적할 수 있습니다.

## 🚀 주요 기능

- **프로젝트 관리**: 여러 프로젝트를 생성하고 독립적으로 관리할 수 있습니다.
- **테스트 케이스 관리 (CMS)**:
  - Suite(폴더) 구조로 케이스 정리
  - 테스트 단계(Steps), 기대 결과, 중요도 설정
- **Excel/CSV Import & Export**:
  - 기존 테스트 케이스를 엑셀로 일괄 업로드 (Import)
  - 작성된 케이스를 엑셀 파일로 백업/공유 (Export)
- **테스트 실행 (Test Runner)**:
  - Test Run을 생성하여 회차별 테스트 수행
  - Pass/Fail/Skip 결과 기록 및 진행률(Progress) 시각화
- **보안 및 권한 관리 (RBAC)**:
  - 회원가입 승인제 (Admin Approval)
  - 역할 기반 접근 제어 (Role-Based Access Control)

## 🔐 역할 및 권한 (Roles)

시스템은 3가지 역할로 구분됩니다.

| 역할 (Role) | 권한 설명 |
|---|---|
| **ADMIN** | **모든 권한**. 사용자 승인 및 권한 변경, 비밀번호 초기화 가능. |
| **QA** | **테스트 관리**. 프로젝트/케이스 생성·수정·삭제, 테스트 실행, Import/Export 가능. |
| **GUEST** | **조회 전용**. 프로젝트 및 케이스 조회, 엑셀 Export(다운로드)만 가능. |

> 회원가입 시 기본 상태는 **승인 대기(PENDING)**이며, 관리자가 승인해야 로그인할 수 있습니다.
> 첫 번째로 가입하는 사용자는 자동으로 **ADMIN** 권한을 갖습니다.

## 🛠 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Database**: SQLite (Prisma ORM)
- **Auth**: JWT (Jose), Bcrypt (Hashing)
- **Styling**: Tailwind CSS v3, Shadcn UI
- **Language**: TypeScript

## 🏁 시작하기

### 1. 설치

```bash
git clone https://github.com/koesnuj/TMS.git
cd TMS/tms
npm install
```

### 2. 데이터베이스 설정

로컬 SQLite 데이터베이스를 초기화합니다.

```bash
npx prisma generate
npx prisma migrate dev --name init
```

### 3. 실행

개발 서버를 실행합니다.

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000) 으로 접속하세요.

## 📂 엑셀 Import 가이드

`Import` 기능을 사용할 때, 엑셀(.xlsx) 파일의 첫 번째 줄(헤더)은 아래 형식을 권장합니다.

| Title | Description | Priority | Steps |
|-------|-------------|----------|-------|
| 로그인 테스트 | 로그인 기능 검증 | High | [{"step":"ID 입력","expected":"입력됨"}] |
| ... | ... | ... | ... |

- **Title**: 필수 입력
- **Priority**: High, Medium, Low 중 하나
- **Steps**: JSON 문자열 또는 일반 텍스트

## 📝 라이선스

MIT License
